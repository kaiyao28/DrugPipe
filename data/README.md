# Data Directory

`data/example/` contains small synthetic TSV files used by the example pipeline
and tests. They are safe to keep in the repository.

Do not commit large downloaded public resources, raw omics matrices, genotype
files or individual-level data. Store those outside the repository and point
DrugPipe commands at local paths.
